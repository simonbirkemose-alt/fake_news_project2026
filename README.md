# fake_news_project2026
We have imported the files we are using (fake news corpus, Liar dataset etc) to a folder called Group_project if you create such a folder parallel to this folder it should be possible to execute the code. In addition to this some of the notebooks have been excecuted in google colab, meaning that to excevute these using the files, you should either redirect the path to a folder containing the relevant files, or use google drive and place the files in a path as shown in the code. other than this we are giving a requirements.txt file so you can get the correct modules for the code.